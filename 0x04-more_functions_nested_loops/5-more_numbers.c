#include "main.h"

/**
* more_numbers - print more numbers
*/

void more_numbers(void)
{
	int a, b;
	for (b = 0; b<= 10; b++)
	{
		if (a > 9)
		{
			_putchar((i / 10) + '0');
		}
		_putchar('\n');
	}
}
